[package]
name = "audio-capture"
edition.workspace = true
version.workspace = true
license.workspace = true
authors.workspace = true
publish.workspace = true

[dependencies]
minutist-common = { path = "../common" }
cpal = { workspace = true }
rubato = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
thiserror = { workspace = true }

# Windows-only: WASAPI communications-mode mic capture (OS beamforming / AEC /
# noise-suppression → processed mono), so we never average a raw mic array.
# See architecture/components.md (audio-capture) + the research artefact
# planning/research/windows-mic-array-capture-2026-06.md.
[target.'cfg(windows)'.dependencies]
wasapi = "0.23"

[dev-dependencies]
tracing-subscriber = { workspace = true }

[features]
test-source = []
