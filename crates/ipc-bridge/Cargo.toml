[package]
name = "ipc-bridge"
edition.workspace = true
version.workspace = true
license.workspace = true
authors.workspace = true

# The ONLY non-app-main crate allowed to import `tauri::*`.
# See architecture/domain-ownership.md — `ipc-bridge` / `systems-engineer` role.

[features]
# GPU acceleration backends (Phase 7), OFF by default. ipc-bridge sits above
# BOTH summariser (its direct dep) and orchestrator (asr + diarizer), so it is
# the single fan-out point that reaches every ML crate. src-tauri forwards here.
vulkan = ["summariser/vulkan", "orchestrator/vulkan", "chat-agent/vulkan", "embedder/vulkan"]
metal = ["summariser/metal", "orchestrator/metal", "chat-agent/metal", "embedder/metal"]
rocm = ["summariser/rocm", "orchestrator/rocm", "chat-agent/rocm", "embedder/rocm"]
cuda = ["summariser/cuda", "orchestrator/cuda", "chat-agent/cuda", "embedder/cuda"]

[dependencies]
async-trait = { workspace = true }
minutist-common = { path = "../common", features = ["specta"] }
orchestrator = { path = "../orchestrator" }
persistence = { path = "../persistence" }
# The notes commands (`save_notes` / `load_notes` / `apply_notes_update` /
# `load_notes_ydoc`) route directly to `NotesStore`, read from the leaf rather
# than through `persistence`'s re-export.
notes-crdt = { path = "../notes-crdt" }
doc-convert = { path = "../doc-convert" }
summariser = { path = "../summariser" }
agent-tools = { path = "../agent-tools" }
chat-agent = { path = "../chat-agent" }
embedder = { path = "../embedder" }
rag-retrieval = { path = "../rag-retrieval" }
settings = { path = "../settings", features = ["specta"] }
chrono = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
specta = { workspace = true }
tauri = { workspace = true }
tauri-plugin-opener = { workspace = true }    # open an attachment original in the host default app
tauri-specta = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
uuid = { workspace = true }  # for MeetingIdType's From impl
sys-locale = { workspace = true }

[dev-dependencies]
llama-cpp-2 = { workspace = true }
specta-typescript = { workspace = true }
tempfile = "3"
tracing-subscriber = { workspace = true }
settings = { path = "../settings" }
# Test-only: the manifest-consistency guard (`tests/default_model_manifest.rs`)
# parses `resources/models.json` to assert the bundled DEFAULT_LLM_MODEL_ID
# stays a real `kind = Llm` entry. This is a dev-dependency, not a production
# edge — `ipc-bridge` still resolves models only through `Orchestrator`
# (no production `model-registry` dependency; see components.md dependency
# table). Mirrors `orchestrator`'s test-only `model-registry` dev-dependency.
model-registry = { path = "../model-registry" }
# For `orchestrator::test_support::test_orchestrator` in the inter-agent driver
# unit test. Not a production edge — the production `orchestrator` dep above
# provides the runtime path; this activates the `test-source` stub only in cfg(test).
orchestrator = { path = "../orchestrator", features = ["test-source"] }
