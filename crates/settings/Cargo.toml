[package]
name = "settings"
edition.workspace = true
version.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
meeting-app-common = { path = "../common" }
serde = { workspace = true }
serde_json = { workspace = true }
specta = { workspace = true, optional = true }
thiserror = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }

[features]
default = []
# When enabled, `Settings` and `Theme` derive `specta::Type` so the
# generated TypeScript bindings can import them directly. Enabled by
# `ipc-bridge` (which itself enables it on `meeting-app-common`).
specta = ["dep:specta", "meeting-app-common/specta"]

[dev-dependencies]
tempfile = "3"
tracing-subscriber = { workspace = true }
